# Set to true to run the UI without the Go backend (in-browser mock data).
VITE_USE_MOCK=true

# Leave empty to use the Vite dev proxy (/api -> http://localhost:8080).
# For a production build, set the full API origin, e.g. https://api.example.com
VITE_API_URL=
